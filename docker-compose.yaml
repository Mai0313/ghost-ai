services:
  app:
    build:
      context: .
      dockerfile: ./docker/Dockerfile
      target: prod
    env_file:
      - .env
    command:
      - python
      - src/ghost_ai/cli.py
    restart: always
    pull_policy: always
